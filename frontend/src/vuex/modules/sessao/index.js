const state = {
  logado: false,
  loader: false,
  auth: {
    login: '',
    senha: '',
    tokenPush: null,
  },
};

const getters = {};

const actions = {};

const mutations = {};

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations,
};
